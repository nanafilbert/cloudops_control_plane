apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: game

resources:
  - namespace.yaml
  - network-policies/default-deny.yaml
  - network-policies/allow-ingress-from-nginx.yaml
  - network-policies/allow-egress-to-rds-dns.yaml
  - network-policies/allow-egress-to-redis.yaml   # <-- add this lines